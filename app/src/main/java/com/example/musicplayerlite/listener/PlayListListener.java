package com.example.musicplayerlite.listener;

import android.content.Context;

import com.example.musicplayerlite.model.Music;

public interface PlayListListener {
    void option(Context context, Music music);
}
